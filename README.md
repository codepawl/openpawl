# TeamClaw App

Monorepo with client and server workspaces.

Quickstart:
1. Install dependencies: npm install
2. Start both dev servers: npm run dev
3. Build: npm run build
4. Run tests: npm run test

See client/ and server/ folders for workspace-specific instructions.
